import { Milolevel } from "../../types/Milolevel";
import { Step, StepType } from "../../types/Step";




export interface Milolevelstatus {
  level: number;
  last_level: number;
  steps:number; 
  currentLevel:number;
}